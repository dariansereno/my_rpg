/*
** EPITECH PROJECT, 2021
** GALAXY
** File description:
** circle_intersections
*/

#include "my_rpg.h"

// sfVector2f intersections_circle(float A, float B, float delta)
// {
//     int x = (-B + )
// }

// sfVector2f *points_circles_intersect(sfVector2i c0, sfVector2i c1,
// int r0, int r1)
// {
//     sfVector2f intersect[2] = {{0, 0}, {0, 0}};
//     int x = c0.x - c1.x;
//     int y = c0.y - c1.y;
//     int N = pow(r1, 2) - pow(r0, 2) - pow(c1.x, 2) + pow(c0.x, 2) - pow(c1.y, 2)
//     + pow(c0.y, 2);
//     float A = pow(x / y, 2) + 1;
//     float B = (2 * (c0.y * (x / y))) - ((2 * N) * (x / y)) - 2 * c0.x;
//     float C = pow(c0.x, 2) + (c0.y, 2) + pow(N, 2) - pow(r0, 2) -
//     ((2 * pow(c0.y, 2)) * N);
//     float delta = sqrt(pow(B, 2) - 4 * (A * C));


// }